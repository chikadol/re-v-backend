# ---- App ----
SPRING_PROFILES_ACTIVE=prod

# 기본값은 로컬(docker), 실제 서버에선 Supabase 값으로 덮어쓰기
SPRING_DATASOURCE_URL=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/revdb}
SPRING_DATASOURCE_USERNAME=${SPRING_DATASOURCE_USERNAME:rev}
SPRING_DATASOURCE_PASSWORD=${SPRING_DATASOURCE_PASSWORD:revpass}

# Flyway 전용(없으면 datasource로 fallback)
SPRING_FLYWAY_URL=${SPRING_FLYWAY_URL:}
SPRING_FLYWAY_USER=${SPRING_FLYWAY_USER:}
SPRING_FLYWAY_PASSWORD=${SPRING_FLYWAY_PASSWORD:}

APP_JWT_SECRET=please-change-this-ultra-long-random-secret

# ---- DB (docker-compose, Supabase에선 무시) ----
POSTGRES_USER=rev
POSTGRES_PASSWORD=revpass
POSTGRES_DB=revdb
DB_PORT=5432