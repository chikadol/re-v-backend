version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: rev
      POSTGRES_USER: rev
      POSTGRES_PASSWORD: rev
    ports:
      - "5432:5432"
    volumes:
      - rev_pgdata:/var/lib/postgresql/data
volumes:
  rev_pgdata:
