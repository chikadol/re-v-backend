# application-prod.yml
spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple&prepareThreshold=0&currentSchema=rev
    username: postgres.gsmbdibjiuwdqhrdvsfo
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.default_schema: rev

  flyway:
    enabled: true
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require
    user: postgres.gsmbdibjiuwdqhrdvsfo
    password: ${DB_PASSWORD}
    schemas: rev
    default-schema: rev
    create-schemas: true
    locations: classpath:db/migration,classpath:db/callback
