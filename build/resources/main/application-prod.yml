spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:chikadol123!}
    driver-class-name: org.postgresql.Driver
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/callback
    default-schema: rev
    schemas: rev
    # url/user/password 설정하지 말 것! (데이터소스 재사용)
    # baseline-on-migrate: true   # 초기 빈 히스토리 문제 있을 때만 잠깐 켜고, 지금은 불필요
jwt:
  issuer: rev-app
  secret: ${JWT_SECRET:d0c41382-cae7-44c3-a401-77828c270ab3}
  access-ttl-seconds: 900        # 15분
  refresh-ttl-seconds: 1209600   # 14일