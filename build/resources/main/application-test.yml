spring:
  flyway:
    enabled: false
    schemas: rev
    baseline-on-migrate: true
    clean-disabled: false
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: rev
        hbm2ddl.create_namespaces: true
