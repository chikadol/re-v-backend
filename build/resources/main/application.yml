spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/krchikadol_db_user
    username: krchikadol_db_user
    password: chikadol123!
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  sql:
    init:
      mode: never

server:
  port: ${PORT:8080}
