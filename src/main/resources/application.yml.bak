spring:
  datasource:
    url: postgresql://postgres:chikadol123!@db.gsmbdibjiuwdqhrdvsfo.supabase.co:5432/postgres
    username: ykkmr's Project
    password: chikadol123!
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    # migrations + callbacks 둘 다 스캔
    locations: classpath:db/migration,classpath:db/callbacks
    default-schema: rev
    schemas: rev
  sql:
    init:
      mode: never

server:
  port: ${PORT:8080}
