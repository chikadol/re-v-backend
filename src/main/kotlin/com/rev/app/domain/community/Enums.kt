package com.rev.app.domain.community

enum class ReactionKind { LIKE, DISLIKE }
enum class ReportStatus { OPEN, REVIEWING, RESOLVED, REJECTED }
enum class ModerationAction { PIN, UNPIN, HIDE, DELETE, RESTORE }
